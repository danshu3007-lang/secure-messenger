# secure-messenger environment variables
# Copy this file to .env and fill in your values
# Never commit the actual .env file

# Directory containing TLS certificates (ca.crt, server.crt, server.key)
MESSENGER_CERT_DIR=/etc/messenger/certs

# Default listen port (optional, defaults to 8443)
# MESSENGER_DEFAULT_PORT=8443
